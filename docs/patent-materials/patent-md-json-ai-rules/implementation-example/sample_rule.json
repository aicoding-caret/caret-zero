{
  "title": "도구 사용 가이드라인",
  "description": "Cline 에이전트의 도구 활용 방법",
  "format": {
    "syntax": "XML",
    "pattern": "<도구_이름><매개변수1>값1</매개변수1><매개변수2>값2</매개변수2>...</도구_이름>"
  },
  "tools": [
    {
      "name": "execute_command",
      "description": "사용자 시스템에서 CLI 명령 실행",
      "parameters": [
        {
          "name": "command",
          "required": true,
          "description": "실행할 CLI 명령"
        },
        {
          "name": "requires_approval",
          "required": true,
          "type": "boolean",
          "description": "명시적 승인 필요 여부"
        }
      ],
      "uses": ["패키지 설치/제거", "파일 시스템 작업", "개발 서버 실행", "빌드 프로세스", "테스트 실행"],
      "cautions": [
        "명령 목적 명확히 설명",
        "예상 결과 설명",
        "파일 시스템 영향 명령은 승인 필요",
        "장시간 실행 가능성 고지"
      ],
      "example": "<execute_command><command>npm install express</command><requires_approval>true</requires_approval></execute_command>"
    },
    {
      "name": "read_file",
      "description": "지정 경로 파일 내용 읽기",
      "parameters": [
        {
          "name": "path",
          "required": true,
          "description": "읽을 파일 경로(작업 디렉토리 기준)"
        }
      ],
      "uses": ["코드 분석", "구성 파일 검토", "텍스트 정보 추출"],
      "cautions": [
        "존재하지 않는 파일 오류 발생",
        "PDF/DOCX 원시 텍스트 자동 추출",
        "바이너리 파일 부적합"
      ],
      "example": "<read_file><path>src/main.js</path></read_file>"
    },
    {
      "name": "write_to_file",
      "description": "파일 생성 또는 덮어쓰기",
      "parameters": [
        {
          "name": "path",
          "required": true,
          "description": "쓸 파일 경로(작업 디렉토리 기준)"
        },
        {
          "name": "content",
          "required": true,
          "description": "파일에 쓸 내용"
        }
      ],
      "uses": ["새 파일 생성", "기존 파일 덮어쓰기", "프로젝트 스캐폴딩"],
      "cautions": [
        "기존 파일 덮어쓰기 주의",
        "전체 내용 제공 필요",
        "필요 디렉토리 자동 생성"
      ],
      "example": "<write_to_file><path>src/config.js</path><content>module.exports = {\n  port: 3000,\n  environment: 'development'\n};</content></write_to_file>"
    }
  ]
}
